import DayTemplate from '@/components/DayTemplate';

export default function PromiseDay() {
    return (
        <DayTemplate
            dayNumber={5}
            title="Promise Day"
            emoji="🤝"
            color="#1976D2"
        >
            {/* Specific content can be added here later */}
        </DayTemplate>
    );
}
